server:
  port: 8080
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/building_energy_standard?useSSL=false&serverTimezone=UTC
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  ai:
#    dashscope:
#      chat:
#        api-key: ${AI_DASHSCOPE_API_KEY}
#        base-url: https://dashscope.aliyuncs.com/compatible-mode/v1
#        options:
#            model: qwen3-vl-8b-instruct
    dashscope:
      api-key: ${OPENAI_API_KEY}
      chat:
        model: qwen-plus
    openai:
      api-key: ${OPENAI_API_KEY}
      chat:
        base-url: https://dashscope.aliyuncs.com/compatible-mode
        options:
          model: qwen-plus
          stream: true
          stream-usage: true
    chat:
      memory:
        repository:
          jdbc:
            initialize-schema: always
            schema: classpath:db/schema-mysql.sql
            platform: mysql
      client:
        enabled: false
    mcp:
      client:
        enabled: true
        type: async
        request-timeout: 120s
        sse:
          connections:
            my-spring-server:
              url: http://localhost:8014
        toolcallback:
          enabled: true


  cache:
    type: caffeine
    caffeine:
      spec: maximumSize=500,expireAfterWrite=3m
  data:
    redis:
      port: 6379
      host: localhost
      password: infini_rag_flow
ragflow:
  base-url: http://localhost:80
  api-key: ragflow-NFFHDntVUE_UwSt6PItGXQINsLOXdQCwqvRceW9Q4Y4
  chat-id: d832c9dc1d0211f189659d8286330dbc
  model: qwen3-max-2026-01-23




weather:
  qweather:
    key: ${QWEATHER_API_KEY}
    base-url: https://mc3aaqqcc7.re.qweatherapi.com


baidu:
  search:
    api-key: ${BAI_DU_API_KEY}  # 百度千帆 API Key
    secret-key: ${BAI_DU_SECRET_KEY}
    base-url: https://qianfan.baidubce.com/v2/ai_search/web_search
  map:
    api-key: ${BAIDU_MAP_API_KEY}

logging:
  level:
    org:
      springframework:
          ai: DEBUG
          client: DEBUG
          openai: DEBUG
  okhttp3: HEADERS
pdf:
  storage:
    path: ./pdf-files           # PDF 文件存放的本地目录

alibaba:
  cloud:
    access-key-id: ${accessKey}
    access-key-secret: ${secretKey}
    bailian:
      workspace-id: ${workspaceId}
      default-index-id: ${indexId}  # 默认知识库ID
      endpoint: bailian.cn-beijing.aliyuncs.com
